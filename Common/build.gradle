plugins {
    id 'java-library'
}

base {
    archivesName = "${mod_id}-common"
}

dependencies {
    // Provided by Minecraft at runtime in both loaders
    compileOnly 'com.google.code.gson:gson:2.11.0'
    compileOnly 'org.slf4j:slf4j-api:2.0.16'
}